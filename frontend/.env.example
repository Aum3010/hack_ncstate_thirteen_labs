# Backend API base URL. Leave empty when using Docker (Vite proxy handles /api).
# When running frontend + backend separately (no Docker):
#   VITE_API_URL=http://localhost:5000
# When using Docker: leave unset or empty (docker-compose sets VITE_API_URL="")
VITE_API_URL=
